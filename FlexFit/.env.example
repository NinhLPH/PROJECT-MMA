# Full API base URL used by the Expo app.
# Android emulator fallback is http://10.0.2.2:5000/api when this value is absent.
# For a physical device, replace localhost with the development machine's LAN IP.
EXPO_PUBLIC_API_URL=http://localhost:5000/api
